
新增特性：

  ---
  一、滑块形状自定义
  
  参数: thumbShape
  类型: ThumbShape 枚举
  默认值: circle
  说明: 内置形状：circle / line（纵向圆角矩形，宽度为4高度为14）

  二、进度条外观自定义

  参数: barBorderRadius
  类型: double? 
  默认值: null
  说明: 进度条圆角半径；非 null 时覆盖 barCapShape 行为，每段两端均应用圆角

  三、正弦波浪线动画

  参数: sineWaveConfig
  类型: SineWaveConfig?
  默认值: null
  说明: 传入则启用已走部分的正弦波浪动画，null 则渲染平直进度条

  SineWaveConfig 子属性：

  ┌──────────────────┬────────┬────────┬────────────────────────────────────────────┐
  │       属性       │  类型  │ 默认值 │                    说明                    │
  ├──────────────────┼────────┼────────┼────────────────────────────────────────────┤
  │ amplitude        │ double │ 3.0    │ 正弦波偏离中心线的峰值幅度                 │
  ├──────────────────┼────────┼────────┼────────────────────────────────────────────┤
  │ cycleCount       │ double │ 2.0    │ 可见进度宽度上的完整周期数                 │
  ├──────────────────┼────────┼────────┼────────────────────────────────────────────┤
  │ speed            │ double │ 1.0    │ 动画速度倍率                               │
  ├──────────────────┼────────┼────────┼────────────────────────────────────────────┤
  │ waveColor        │ Color? │ null   │ 波浪颜色覆盖，null 时使用 progressBarColor │
  ├──────────────────┼────────┼────────┼────────────────────────────────────────────┤
  │ waveCount        │ int    │ 1      │ 叠加波浪层数，>1 时产生丰富波纹效果        │
  ├──────────────────┼────────┼────────┼────────────────────────────────────────────┤
  │ clampToBarBounds │ bool   │ false  │ 是否将波浪裁剪到进度条边界内               │
  └──────────────────┴────────┴────────┴────────────────────────────────────────────┘

  波浪模式下，已走部分仅显示正弦曲线，后方剩余部分显示平直底色条，视觉上不重叠。

  四、滑块间隙

  参数: thumbGap
  类型: double
  默认值: 0.0
  说明: 滑块周围间隙，进度条在滑块处断开为左右两段，每段两端均应用圆角，同步barBorderRadius
  ────────────────────────────────────────
  参数: thumbBarGap
  类型: double
  默认值: 0.0
  说明: 进度条两端与滑块之间的视觉间隙（继承自原参数列表）
  
  
  五、向后兼容

  所有新增参数均有合理默认值（sineWaveConfig = null、thumbShape = circle、thumbGap = 
  0、barBorderRadius = null、thumbWidget = null），仅使用 example.dart
  原有参数构造时，行为与视觉完全一致。

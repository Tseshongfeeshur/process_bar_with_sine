name: process_bar_with_sine
description: 一个增强版 Flutter 进度条组件，支持正弦波浪线动画、可定义滑块形状和进度条样式。
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

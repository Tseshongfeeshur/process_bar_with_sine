name: process_bar_with_sine
description: An enhanced Flutter progress bar, featuring dynamic sine-wave animations.
repository: https://github.com/Tseshongfeeshur/process_bar_with_sine
version: 1.2.2

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
